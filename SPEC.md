# 本地AI智能浏览器 · 产品规格文档

> 本文档为产品功能规格说明，详细技术实现见 [README.md](README.md)。

---

## 📋 产品概述

**产品名称：** 本地AI智能浏览器
**产品类型：** Windows 桌面应用程序
**核心定位：** 离线优先、AI 增强的浏览器，数据本地存储，无互联网账户依赖
**目标用户：** 注重隐私安全、需要 AI 辅助浏览的个人用户

---

## 🎯 核心价值主张

### 1. 数据完全本地化
- 收藏夹、浏览历史、AI 模型配置全部存储在本地
- 无需注册账户、无需联网
- 卸载即删除，无残留

### 2. AI 驱动的浏览器交互
- 用自然语言代替传统点击操作
- AI 理解指令并调用 MCP 工具执行浏览器操作
- 降低操作复杂度，提升浏览效率

### 3. 主流模型自由切换
- 支持 MiniMax、DeepSeek、Qwen 三大主流模型
- 用户自行配置 API Key，灵活切换
- MCP 协议标准，扩展性强

---

## 🖥️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层                              │
│  ┌─────────┐  ┌────────────┐  ┌────────────────────┐    │
│  │ 地址栏  │  │ AI 对话面板 │  │   侧边栏           │    │
│  │         │  │            │  │  (收藏/历史)        │    │
│  └────┬────┘  └─────┬──────┘  └─────────┬──────────┘    │
│       │            │                    │                │
│       ▼            ▼                    ▼                │
│  ┌─────────────────────────────────────────────┐        │
│  │              IPC 通信层                       │        │
│  │  (Electron contextBridge 安全桥接)           │        │
│  └─────────────────────────────────────────────┘        │
│                      │                                   │
│  ┌───────────────────┴────────────────────────┐         │
│  │           Electron 主进程                   │         │
│  │  ┌──────────┐ ┌──────────┐ ┌─────────┐   │         │
│  │  │ 窗口管理  │ │  IPC     │ │ 数据存储 │   │         │
│  │  │          │ │ 处理器   │ │(JSON)   │   │         │
│  │  └──────────┘ └──────────┘ └─────────┘   │         │
│  └───────────────────────────────────────────┘         │
│                         │                              │
│  ┌──────────────────────┴───────────────────────┐      │
│  │            Chromium 内核 (WebView)            │      │
│  │  页面渲染 · JavaScript 执行 · DOM 操作         │      │
│  └───────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 功能模块

### 1. 浏览器核心功能

| 功能 | 描述 | 状态 |
|------|------|------|
| 地址栏导航 | 支持 URL 直接访问和搜索引擎搜索 | ✅ 已实现 |
| Chromium 内核 | WebView 渲染互联网页面 | ✅ 已实现 |
| 前进/后退 | 访问历史导航（UI 占位） | 🟡 简化实现 |
| 页面刷新 | 刷新当前页面 | ✅ 已实现 |
| 新标签页 | 在新标签打开链接 | ✅ AI 指令支持 |

### 2. 本地数据管理

| 功能 | 描述 | 数据路径 |
|------|------|---------|
| 收藏夹 | 增删改查，支持文件夹分组 | `%APPDATA%/LocalAIBrowser/` |
| 浏览历史 | 按时间线展示，可搜索 | 同上 |
| AI 模型配置 | API Key + 端点管理 | 同上 |

### 3. AI 对话交互（MCP 客户端）

#### MCP 工具定义

| 工具名称 | 触发指令 | 功能说明 |
|---------|---------|---------|
| `open_urls` | 「打开链接」 | 在多个新标签中打开 URL 列表 |
| `summarize_page` | 「总结页面」 | 总结当前页面内容 |
| `add_bookmark` | 「收藏页面」 | 添加当前页面到收藏夹 |
| `list_bookmarks` | 「查看收藏」 | 列出所有收藏 |
| `list_history` | 「查看历史」 | 列出浏览历史 |

#### AI 响应流程

```
用户输入自然语言
       │
       ▼
解析用户意图 ───────────────────┐
       │                          │
       ▼                          ▼
调用 AI 模型                  指令匹配
（携带页面 URL 上下文）        │
       │                        ▼
       ▼                   执行 MCP 工具
返回 AI 响应                   │
（含 [TOOL:xxx:params] 格式）  │
       │                        │
       ▼                        ▼
解析工具调用 ◄──────────────────┘
       │
       ▼
浏览器执行操作
```

### 4. 模型配置管理

| 功能 | 描述 |
|------|------|
| 添加模型 | 填入名称、端点 URL、API Key |
| 删除模型 | 移除模型配置 |
| 设为默认 | 选择默认调用模型 |
| 测试连接 | 验证 API Key 有效性 |

---

## 🎨 UI 设计规范

### 窗口布局

```
┌─────────────────────────────────────────────────────────┐
│  标题栏（应用名称 + 窗口控件）               [AI] 按钮   │
├─────────────────────────────────────────────────────────┤
│  地址栏 + 导航按钮（后退/前进/刷新）                      │
├───────────┬─────────────────────────┬────────────────────┤
│           │                         │                    │
│  侧边栏    │    主内容区              │    AI 面板         │
│  收藏/历史  │    (WebView)            │    (可折叠)        │
│  (240px)   │                         │    (360px)        │
│           │                         │                    │
└───────────┴─────────────────────────┴────────────────────┘
```

### 主题色彩

| 变量 | 色值 | 用途 |
|------|------|------|
| `--bg-primary` | `#F5F7FA` | 主背景色（浅灰） |
| `--bg-secondary` | `#FFFFFF` | 卡片/面板背景 |
| `--accent-blue` | `#4A90E2` | 强调色（AI 蓝） |
| `--accent-ai` | `#00D4FF` | AI 高亮色（青色） |
| `--text-primary` | `#1A202C` | 主文字色 |
| `--text-muted` | `#888888` | 次要文字色 |

---

## 🔐 安全设计

| 设计点 | 实现方式 |
|--------|---------|
| 上下文隔离 | `contextIsolation: true` |
| 禁用 Node 集成 | `nodeIntegration: false` |
| 预加载脚本桥接 | 通过 `contextBridge` 安全暴露 API |
| 外部链接 | 通过 `shell.openExternal` 打开（安全提示） |
| CSP 策略 | Content Security Policy 限制资源加载 |

---

## 📊 验收标准

| 编号 | 验收项 | 优先级 |
|------|--------|--------|
| 1 | 启动后主界面正常渲染（地址栏 + 侧边栏 + WebView） | P0 |
| 2 | 可访问互联网页面 | P0 |
| 3 | 侧边栏收藏功能可用（增/删/查） | P0 |
| 4 | AI 面板可调用配置好的模型 | P0 |
| 5 | AI 指令「打开这个页面的所有链接」正常工作 | P1 |
| 6 | 构建产出 `.exe` 文件 | P0 |
| 7 | GitHub 仓库创建完成并推送代码 | P0 |

---

## 🚧 未来规划

- [ ] 前进/后退按钮完整功能
- [ ] 多标签页支持
- [ ] 书签导入/导出
- [ ] 暗黑主题支持
- [ ] 模型对话历史
- [ ] 页面内容 AI 阅读模式
- [ ] MAC/Linux 跨平台支持

---

## 📄 附录

### A. 数据存储格式

**收藏夹/历史/模型配置** 均以 JSON 格式存储在：
```
%APPDATA%/LocalAIBrowser/
└── browser-data.json
```

### B. MCP 工具调用协议

AI 返回格式示例：
```
[TOOL:open_urls:{"urls":["https://example.com","https://test.com"]}]
[TOOL:add_bookmark:{"title":"示例页面","url":"https://example.com"}]
```

### C. 快速命令参考

```bash
# 开发
pnpm run dev

# 构建（设置镜像）
ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ pnpm run dist

# 清理
rm -rf dist dist-electron release node_modules
```